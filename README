example usage:
    make
   ./casm -i examples/adder.v -o prog
   ./vcpu -r prog  #runs the program




with cycle limiting to 50 cycles/second, the -v options prints the registers at each cycle:
   ./vcpu -c 50 -v -r prog  

